# ⚡ Быстрый старт

Начни работу за 1 минуту с помощью ОДНОЙ команды!

> 🇬🇧 [English version](QUICK_START.md)

---

## 🚀 Запуск одной командой (Рекомендуется)

### Шаг 1: Скачать

```bash
git clone https://github.com/Adrena1ine-ai/AI-Native_Project_Scaffolding.git
cd AI-Native_Project_Scaffolding
```

### Шаг 2: Запустить ОДНУ команду!

**Windows (PowerShell):**
```powershell
.\start.ps1
```

**Linux/macOS:**
```bash
./start.sh
```

### Шаг 3: Готово! 🎉

Браузер откроется автоматически с:

1. **Welcome Screen** → Выбери язык (Русский/English)
2. **Dashboard** → Создавай проекты, очищай, проверяй

---

## 📸 Что ты увидишь

### Welcome Screen (Первый запуск)
```
🛠️ AI-Native Project Scaffolding

🌍 Select language / Выберите язык

🇬🇧 English          🇷🇺 Русский
```

### Главная Dashboard
| Страница | Описание |
|----------|----------|
| 🏠 Главная | Быстрые действия, статистика |
| 🆕 Создать | Визуальный конструктор проектов |
| 🧹 Очистка | Анализ и исправление проектов |
| 🏥 Health | Проверка конфигурации |
| ⚙️ Настройки | Выбор IDE по умолчанию |
| ❓ Помощь | Документация для новичков |

---

## 💻 Альтернативная установка

### Из PyPI

```bash
# Установка
pip install ai-toolkit[web]

# Запуск Dashboard
ai-toolkit dashboard

# Или интерактивный CLI
ai-toolkit
```

### Вручную из исходников

```bash
cd AI-Native_Project_Scaffolding
pip install -e ".[web]"
python -m web.app
```

---

## 🛠️ Создание первого проекта

### Через Dashboard (Самый простой способ)

1. Открой Dashboard: http://127.0.0.1:8080
2. Нажми **"🆕 Создать"**
3. Введи имя проекта: `my_bot`
4. Выбери шаблон: **bot**
5. Выбери IDE: **Cursor** (или твою IDE)
6. Нажми **"Создать проект"** ✅

### Через CLI

```bash
ai-toolkit create my_bot --template bot
```

### После создания

```bash
cd my_bot

# Создать venv ВНЕ проекта (ключевая фича!)
./scripts/bootstrap.sh          # Linux/macOS
.\scripts\bootstrap.ps1         # Windows

# Активировать venv
source ../_venvs/my_bot-venv/bin/activate    # Linux/macOS
..\_venvs\my_bot-venv\Scripts\activate       # Windows

# Настроить и запустить
cp .env.example .env
python main.py
```

---

## 📦 Доступные шаблоны

| Шаблон | Описание | Что создаётся |
|--------|----------|---------------|
| 🤖 `bot` | Telegram бот | aiogram 3.x, handlers, keyboards |
| 🌐 `webapp` | Mini App | HTML/CSS/JS, API |
| ⚡ `fastapi` | REST API | FastAPI, SQLAlchemy |
| 🕷️ `parser` | Веб парсер | aiohttp, BeautifulSoup |
| 🚀 `full` | Всё вместе | bot + webapp + api + parser |
| 📦 `monorepo` | Несколько проектов | apps/, packages/, shared/ |

---

## 🔧 Работа с существующими проектами

### Очистка (Исправить грязный проект)

```bash
# Через Dashboard
# → Перейди в "🧹 Очистка" → Введи путь → Анализировать

# Через CLI
ai-toolkit cleanup ./my_project --level safe     # Только анализ
ai-toolkit cleanup ./my_project --level medium   # Исправить с бэкапом
```

### Health Check

```bash
ai-toolkit health ./my_project
```

### Добавить Toolkit в существующий проект

```bash
ai-toolkit migrate ./my_project
```

---

## 🖥️ Поддерживаемые IDE

| IDE | Конфиг файлы | Автоопределение |
|-----|--------------|-----------------|
| 💜 **Cursor** | `.cursorrules`, `.cursorignore` | ✅ |
| 💙 **GitHub Copilot** | `.github/copilot-instructions.md` | ✅ |
| 🟢 **Claude** | `CLAUDE.md` | ✅ |
| 🌊 **Windsurf** | `.windsurfrules` | ✅ |

> 💡 Dashboard **автоматически определяет** какие IDE настроены в проекте!

---

## ❓ Решение проблем

### Порт уже занят

```bash
# Использовать другой порт
.\start.ps1 -Port 3000          # Windows
./start.sh --port 3000          # Linux/macOS

# Или вручную
python -m web.app --port 3000
```

### Команда `ai-toolkit` не найдена

```bash
# Используй синтаксис Python модуля
python -m src.cli              # CLI
python -m web.app              # Dashboard
```

### Неправильная директория

> ⚠️ Всегда запускай из папки `AI-Native_Project_Scaffolding`!

```bash
cd AI-Native_Project_Scaffolding
.\start.ps1
```

---

## 📚 Узнать больше

- 📖 [Полное руководство](GUIDE.ru.md) — подробная документация
- ❓ [FAQ](FAQ.ru.md) — частые вопросы
- 🐙 [GitHub](https://github.com/Adrena1ine-ai/AI-Native_Project_Scaffolding) — поставь звезду ⭐
- 📱 [Telegram](https://t.me/MichaelSalmin) — получить помощь

---

## 🎯 Магия: venv ВНЕ проекта

```
projects/
├── _venvs/                  ← Все venv здесь!
│   └── my_bot-venv/         (не в проекте!)
│
└── my_bot/                  ← Чистый проект!
    ├── _AI_INCLUDE/         ← Правила для AI
    ├── scripts/
    │   └── bootstrap.sh     ← Создаёт venv снаружи
    └── main.py
```

**Результат:** Быстрая IDE, умный AI, чистый Git! 🚀

Начни работу за 1 минуту с помощью ОДНОЙ команды!

> 🇬🇧 [English version](QUICK_START.md)

---

## 🚀 Запуск одной командой (Рекомендуется)

### Шаг 1: Скачать

```bash
git clone https://github.com/Adrena1ine-ai/AI-Native_Project_Scaffolding.git
cd AI-Native_Project_Scaffolding
```

### Шаг 2: Запустить ОДНУ команду!

**Windows (PowerShell):**
```powershell
.\start.ps1
```

**Linux/macOS:**
```bash
./start.sh
```

### Шаг 3: Готово! 🎉

Браузер откроется автоматически с:

1. **Welcome Screen** → Выбери язык (Русский/English)
2. **Dashboard** → Создавай проекты, очищай, проверяй

---

## 📸 Что ты увидишь

### Welcome Screen (Первый запуск)
```
🛠️ AI-Native Project Scaffolding

🌍 Select language / Выберите язык

🇬🇧 English          🇷🇺 Русский
```

### Главная Dashboard
| Страница | Описание |
|----------|----------|
| 🏠 Главная | Быстрые действия, статистика |
| 🆕 Создать | Визуальный конструктор проектов |
| 🧹 Очистка | Анализ и исправление проектов |
| 🏥 Health | Проверка конфигурации |
| ⚙️ Настройки | Выбор IDE по умолчанию |
| ❓ Помощь | Документация для новичков |

---

## 💻 Альтернативная установка

### Из PyPI

```bash
# Установка
pip install ai-toolkit[web]

# Запуск Dashboard
ai-toolkit dashboard

# Или интерактивный CLI
ai-toolkit
```

### Вручную из исходников

```bash
cd AI-Native_Project_Scaffolding
pip install -e ".[web]"
python -m web.app
```

---

## 🛠️ Создание первого проекта

### Через Dashboard (Самый простой способ)

1. Открой Dashboard: http://127.0.0.1:8080
2. Нажми **"🆕 Создать"**
3. Введи имя проекта: `my_bot`
4. Выбери шаблон: **bot**
5. Выбери IDE: **Cursor** (или твою IDE)
6. Нажми **"Создать проект"** ✅

### Через CLI

```bash
ai-toolkit create my_bot --template bot
```

### После создания

```bash
cd my_bot

# Создать venv ВНЕ проекта (ключевая фича!)
./scripts/bootstrap.sh          # Linux/macOS
.\scripts\bootstrap.ps1         # Windows

# Активировать venv
source ../_venvs/my_bot-venv/bin/activate    # Linux/macOS
..\_venvs\my_bot-venv\Scripts\activate       # Windows

# Настроить и запустить
cp .env.example .env
python main.py
```

---

## 📦 Доступные шаблоны

| Шаблон | Описание | Что создаётся |
|--------|----------|---------------|
| 🤖 `bot` | Telegram бот | aiogram 3.x, handlers, keyboards |
| 🌐 `webapp` | Mini App | HTML/CSS/JS, API |
| ⚡ `fastapi` | REST API | FastAPI, SQLAlchemy |
| 🕷️ `parser` | Веб парсер | aiohttp, BeautifulSoup |
| 🚀 `full` | Всё вместе | bot + webapp + api + parser |
| 📦 `monorepo` | Несколько проектов | apps/, packages/, shared/ |

---

## 🔧 Работа с существующими проектами

### Очистка (Исправить грязный проект)

```bash
# Через Dashboard
# → Перейди в "🧹 Очистка" → Введи путь → Анализировать

# Через CLI
ai-toolkit cleanup ./my_project --level safe     # Только анализ
ai-toolkit cleanup ./my_project --level medium   # Исправить с бэкапом
```

### Health Check

```bash
ai-toolkit health ./my_project
```

### Добавить Toolkit в существующий проект

```bash
ai-toolkit migrate ./my_project
```

---

## 🖥️ Поддерживаемые IDE

| IDE | Конфиг файлы | Автоопределение |
|-----|--------------|-----------------|
| 💜 **Cursor** | `.cursorrules`, `.cursorignore` | ✅ |
| 💙 **GitHub Copilot** | `.github/copilot-instructions.md` | ✅ |
| 🟢 **Claude** | `CLAUDE.md` | ✅ |
| 🌊 **Windsurf** | `.windsurfrules` | ✅ |

> 💡 Dashboard **автоматически определяет** какие IDE настроены в проекте!

---

## ❓ Решение проблем

### Порт уже занят

```bash
# Использовать другой порт
.\start.ps1 -Port 3000          # Windows
./start.sh --port 3000          # Linux/macOS

# Или вручную
python -m web.app --port 3000
```

### Команда `ai-toolkit` не найдена

```bash
# Используй синтаксис Python модуля
python -m src.cli              # CLI
python -m web.app              # Dashboard
```

### Неправильная директория

> ⚠️ Всегда запускай из папки `AI-Native_Project_Scaffolding`!

```bash
cd AI-Native_Project_Scaffolding
.\start.ps1
```

---

## 📚 Узнать больше

- 📖 [Полное руководство](GUIDE.ru.md) — подробная документация
- ❓ [FAQ](FAQ.ru.md) — частые вопросы
- 🐙 [GitHub](https://github.com/Adrena1ine-ai/AI-Native_Project_Scaffolding) — поставь звезду ⭐
- 📱 [Telegram](https://t.me/MichaelSalmin) — получить помощь

---

## 🎯 Магия: venv ВНЕ проекта

```
projects/
├── _venvs/                  ← Все venv здесь!
│   └── my_bot-venv/         (не в проекте!)
│
└── my_bot/                  ← Чистый проект!
    ├── _AI_INCLUDE/         ← Правила для AI
    ├── scripts/
    │   └── bootstrap.sh     ← Создаёт venv снаружи
    └── main.py
```

**Результат:** Быстрая IDE, умный AI, чистый Git! 🚀



Начни работу за 1 минуту с помощью ОДНОЙ команды!

> 🇬🇧 [English version](QUICK_START.md)

---

## 🚀 Запуск одной командой (Рекомендуется)

### Шаг 1: Скачать

```bash
git clone https://github.com/Adrena1ine-ai/AI-Native_Project_Scaffolding.git
cd AI-Native_Project_Scaffolding
```

### Шаг 2: Запустить ОДНУ команду!

**Windows (PowerShell):**
```powershell
.\start.ps1
```

**Linux/macOS:**
```bash
./start.sh
```

### Шаг 3: Готово! 🎉

Браузер откроется автоматически с:

1. **Welcome Screen** → Выбери язык (Русский/English)
2. **Dashboard** → Создавай проекты, очищай, проверяй

---

## 📸 Что ты увидишь

### Welcome Screen (Первый запуск)
```
🛠️ AI-Native Project Scaffolding

🌍 Select language / Выберите язык

🇬🇧 English          🇷🇺 Русский
```

### Главная Dashboard
| Страница | Описание |
|----------|----------|
| 🏠 Главная | Быстрые действия, статистика |
| 🆕 Создать | Визуальный конструктор проектов |
| 🧹 Очистка | Анализ и исправление проектов |
| 🏥 Health | Проверка конфигурации |
| ⚙️ Настройки | Выбор IDE по умолчанию |
| ❓ Помощь | Документация для новичков |

---

## 💻 Альтернативная установка

### Из PyPI

```bash
# Установка
pip install ai-toolkit[web]

# Запуск Dashboard
ai-toolkit dashboard

# Или интерактивный CLI
ai-toolkit
```

### Вручную из исходников

```bash
cd AI-Native_Project_Scaffolding
pip install -e ".[web]"
python -m web.app
```

---

## 🛠️ Создание первого проекта

### Через Dashboard (Самый простой способ)

1. Открой Dashboard: http://127.0.0.1:8080
2. Нажми **"🆕 Создать"**
3. Введи имя проекта: `my_bot`
4. Выбери шаблон: **bot**
5. Выбери IDE: **Cursor** (или твою IDE)
6. Нажми **"Создать проект"** ✅

### Через CLI

```bash
ai-toolkit create my_bot --template bot
```

### После создания

```bash
cd my_bot

# Создать venv ВНЕ проекта (ключевая фича!)
./scripts/bootstrap.sh          # Linux/macOS
.\scripts\bootstrap.ps1         # Windows

# Активировать venv
source ../_venvs/my_bot-venv/bin/activate    # Linux/macOS
..\_venvs\my_bot-venv\Scripts\activate       # Windows

# Настроить и запустить
cp .env.example .env
python main.py
```

---

## 📦 Доступные шаблоны

| Шаблон | Описание | Что создаётся |
|--------|----------|---------------|
| 🤖 `bot` | Telegram бот | aiogram 3.x, handlers, keyboards |
| 🌐 `webapp` | Mini App | HTML/CSS/JS, API |
| ⚡ `fastapi` | REST API | FastAPI, SQLAlchemy |
| 🕷️ `parser` | Веб парсер | aiohttp, BeautifulSoup |
| 🚀 `full` | Всё вместе | bot + webapp + api + parser |
| 📦 `monorepo` | Несколько проектов | apps/, packages/, shared/ |

---

## 🔧 Работа с существующими проектами

### Очистка (Исправить грязный проект)

```bash
# Через Dashboard
# → Перейди в "🧹 Очистка" → Введи путь → Анализировать

# Через CLI
ai-toolkit cleanup ./my_project --level safe     # Только анализ
ai-toolkit cleanup ./my_project --level medium   # Исправить с бэкапом
```

### Health Check

```bash
ai-toolkit health ./my_project
```

### Добавить Toolkit в существующий проект

```bash
ai-toolkit migrate ./my_project
```

---

## 🖥️ Поддерживаемые IDE

| IDE | Конфиг файлы | Автоопределение |
|-----|--------------|-----------------|
| 💜 **Cursor** | `.cursorrules`, `.cursorignore` | ✅ |
| 💙 **GitHub Copilot** | `.github/copilot-instructions.md` | ✅ |
| 🟢 **Claude** | `CLAUDE.md` | ✅ |
| 🌊 **Windsurf** | `.windsurfrules` | ✅ |

> 💡 Dashboard **автоматически определяет** какие IDE настроены в проекте!

---

## ❓ Решение проблем

### Порт уже занят

```bash
# Использовать другой порт
.\start.ps1 -Port 3000          # Windows
./start.sh --port 3000          # Linux/macOS

# Или вручную
python -m web.app --port 3000
```

### Команда `ai-toolkit` не найдена

```bash
# Используй синтаксис Python модуля
python -m src.cli              # CLI
python -m web.app              # Dashboard
```

### Неправильная директория

> ⚠️ Всегда запускай из папки `AI-Native_Project_Scaffolding`!

```bash
cd AI-Native_Project_Scaffolding
.\start.ps1
```

---

## 📚 Узнать больше

- 📖 [Полное руководство](GUIDE.ru.md) — подробная документация
- ❓ [FAQ](FAQ.ru.md) — частые вопросы
- 🐙 [GitHub](https://github.com/Adrena1ine-ai/AI-Native_Project_Scaffolding) — поставь звезду ⭐
- 📱 [Telegram](https://t.me/MichaelSalmin) — получить помощь

---

## 🎯 Магия: venv ВНЕ проекта

```
projects/
├── _venvs/                  ← Все venv здесь!
│   └── my_bot-venv/         (не в проекте!)
│
└── my_bot/                  ← Чистый проект!
    ├── _AI_INCLUDE/         ← Правила для AI
    ├── scripts/
    │   └── bootstrap.sh     ← Создаёт venv снаружи
    └── main.py
```

**Результат:** Быстрая IDE, умный AI, чистый Git! 🚀

Начни работу за 1 минуту с помощью ОДНОЙ команды!

> 🇬🇧 [English version](QUICK_START.md)

---

## 🚀 Запуск одной командой (Рекомендуется)

### Шаг 1: Скачать

```bash
git clone https://github.com/Adrena1ine-ai/AI-Native_Project_Scaffolding.git
cd AI-Native_Project_Scaffolding
```

### Шаг 2: Запустить ОДНУ команду!

**Windows (PowerShell):**
```powershell
.\start.ps1
```

**Linux/macOS:**
```bash
./start.sh
```

### Шаг 3: Готово! 🎉

Браузер откроется автоматически с:

1. **Welcome Screen** → Выбери язык (Русский/English)
2. **Dashboard** → Создавай проекты, очищай, проверяй

---

## 📸 Что ты увидишь

### Welcome Screen (Первый запуск)
```
🛠️ AI-Native Project Scaffolding

🌍 Select language / Выберите язык

🇬🇧 English          🇷🇺 Русский
```

### Главная Dashboard
| Страница | Описание |
|----------|----------|
| 🏠 Главная | Быстрые действия, статистика |
| 🆕 Создать | Визуальный конструктор проектов |
| 🧹 Очистка | Анализ и исправление проектов |
| 🏥 Health | Проверка конфигурации |
| ⚙️ Настройки | Выбор IDE по умолчанию |
| ❓ Помощь | Документация для новичков |

---

## 💻 Альтернативная установка

### Из PyPI

```bash
# Установка
pip install ai-toolkit[web]

# Запуск Dashboard
ai-toolkit dashboard

# Или интерактивный CLI
ai-toolkit
```

### Вручную из исходников

```bash
cd AI-Native_Project_Scaffolding
pip install -e ".[web]"
python -m web.app
```

---

## 🛠️ Создание первого проекта

### Через Dashboard (Самый простой способ)

1. Открой Dashboard: http://127.0.0.1:8080
2. Нажми **"🆕 Создать"**
3. Введи имя проекта: `my_bot`
4. Выбери шаблон: **bot**
5. Выбери IDE: **Cursor** (или твою IDE)
6. Нажми **"Создать проект"** ✅

### Через CLI

```bash
ai-toolkit create my_bot --template bot
```

### После создания

```bash
cd my_bot

# Создать venv ВНЕ проекта (ключевая фича!)
./scripts/bootstrap.sh          # Linux/macOS
.\scripts\bootstrap.ps1         # Windows

# Активировать venv
source ../_venvs/my_bot-venv/bin/activate    # Linux/macOS
..\_venvs\my_bot-venv\Scripts\activate       # Windows

# Настроить и запустить
cp .env.example .env
python main.py
```

---

## 📦 Доступные шаблоны

| Шаблон | Описание | Что создаётся |
|--------|----------|---------------|
| 🤖 `bot` | Telegram бот | aiogram 3.x, handlers, keyboards |
| 🌐 `webapp` | Mini App | HTML/CSS/JS, API |
| ⚡ `fastapi` | REST API | FastAPI, SQLAlchemy |
| 🕷️ `parser` | Веб парсер | aiohttp, BeautifulSoup |
| 🚀 `full` | Всё вместе | bot + webapp + api + parser |
| 📦 `monorepo` | Несколько проектов | apps/, packages/, shared/ |

---

## 🔧 Работа с существующими проектами

### Очистка (Исправить грязный проект)

```bash
# Через Dashboard
# → Перейди в "🧹 Очистка" → Введи путь → Анализировать

# Через CLI
ai-toolkit cleanup ./my_project --level safe     # Только анализ
ai-toolkit cleanup ./my_project --level medium   # Исправить с бэкапом
```

### Health Check

```bash
ai-toolkit health ./my_project
```

### Добавить Toolkit в существующий проект

```bash
ai-toolkit migrate ./my_project
```

---

## 🖥️ Поддерживаемые IDE

| IDE | Конфиг файлы | Автоопределение |
|-----|--------------|-----------------|
| 💜 **Cursor** | `.cursorrules`, `.cursorignore` | ✅ |
| 💙 **GitHub Copilot** | `.github/copilot-instructions.md` | ✅ |
| 🟢 **Claude** | `CLAUDE.md` | ✅ |
| 🌊 **Windsurf** | `.windsurfrules` | ✅ |

> 💡 Dashboard **автоматически определяет** какие IDE настроены в проекте!

---

## ❓ Решение проблем

### Порт уже занят

```bash
# Использовать другой порт
.\start.ps1 -Port 3000          # Windows
./start.sh --port 3000          # Linux/macOS

# Или вручную
python -m web.app --port 3000
```

### Команда `ai-toolkit` не найдена

```bash
# Используй синтаксис Python модуля
python -m src.cli              # CLI
python -m web.app              # Dashboard
```

### Неправильная директория

> ⚠️ Всегда запускай из папки `AI-Native_Project_Scaffolding`!

```bash
cd AI-Native_Project_Scaffolding
.\start.ps1
```

---

## 📚 Узнать больше

- 📖 [Полное руководство](GUIDE.ru.md) — подробная документация
- ❓ [FAQ](FAQ.ru.md) — частые вопросы
- 🐙 [GitHub](https://github.com/Adrena1ine-ai/AI-Native_Project_Scaffolding) — поставь звезду ⭐
- 📱 [Telegram](https://t.me/MichaelSalmin) — получить помощь

---

## 🎯 Магия: venv ВНЕ проекта

```
projects/
├── _venvs/                  ← Все venv здесь!
│   └── my_bot-venv/         (не в проекте!)
│
└── my_bot/                  ← Чистый проект!
    ├── _AI_INCLUDE/         ← Правила для AI
    ├── scripts/
    │   └── bootstrap.sh     ← Создаёт venv снаружи
    └── main.py
```

**Результат:** Быстрая IDE, умный AI, чистый Git! 🚀
