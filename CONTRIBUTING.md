# 🤝 Руководство по участию в проекте

Спасибо за интерес к AI Toolkit! Мы рады любому вкладу.

---

## 📋 Содержание

- [Как помочь](#-как-помочь)
- [Сообщить о баге](#-сообщить-о-баге)
- [Предложить фичу](#-предложить-фичу)
- [Настройка окружения](#-настройка-окружения)
- [Стиль кода](#-стиль-кода)
- [Процесс Pull Request](#-процесс-pull-request)
- [Структура проекта](#-структура-проекта)

---

## 💡 Как помочь

Есть много способов внести вклад:

| Способ | Описание |
|--------|----------|
| 🐛 **Баги** | Сообщить о найденном баге |
| 💡 **Фичи** | Предложить новую функцию |
| 📖 **Документация** | Улучшить документацию |
| 🌍 **Перевод** | Перевести на другой язык |
| 🧪 **Тесты** | Добавить тесты |
| 🔧 **Код** | Исправить баг или добавить фичу |

---

## 🐛 Сообщить о баге

1. Проверь, что баг ещё не был [сообщён](https://github.com/mickhael/ai-toolkit/issues)
2. Создай [новый Issue](https://github.com/mickhael/ai-toolkit/issues/new)
3. Используй шаблон:

```markdown
### Описание бага
Что происходит не так?

### Ожидаемое поведение
Что должно происходить?

### Шаги воспроизведения
1. Запустить `ai-toolkit create test`
2. ...

### Окружение
- OS: Ubuntu 22.04
- Python: 3.11
- AI Toolkit: 3.0.0

### Логи/скриншоты
(если есть)
```

---

## 💡 Предложить фичу

1. Проверь [Roadmap](README.md#-roadmap) — может уже планируется
2. Создай [Feature Request](https://github.com/mickhael/ai-toolkit/issues/new?template=feature_request.md)
3. Опиши:
   - **Проблему** которую решает фича
   - **Решение** которое ты предлагаешь
   - **Альтернативы** если есть

---

## 🔧 Настройка окружения

### 1. Fork и Clone

```bash
# Fork через GitHub UI, затем:
git clone https://github.com/YOUR_USERNAME/ai-toolkit.git
cd ai-toolkit
```

### 2. Создать venv

```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate     # Windows
```

### 3. Установить зависимости

```bash
pip install -e ".[dev,web]"
```

### 4. Настроить pre-commit

```bash
pre-commit install
```

### 5. Проверить что всё работает

```bash
pytest
mypy src
ruff check src
```

---

## 📝 Стиль кода

### Python

- **Python 3.10+** — используем современный синтаксис
- **Type hints** — все функции должны быть типизированы
- **Docstrings** — Google style
- **Форматирование** — `ruff format`
- **Линтинг** — `ruff check`

```python
from __future__ import annotations

from pathlib import Path
from typing import Any


def create_project(
    name: str,
    path: Path,
    *,
    template: str = "bot",
    include_docker: bool = True,
) -> bool:
    """
    Создаёт новый проект.
    
    Args:
        name: Название проекта
        path: Путь для создания
        template: Шаблон проекта
        include_docker: Добавить Docker файлы
        
    Returns:
        True если успешно, False иначе
        
    Raises:
        ValueError: Если имя проекта невалидно
    """
    ...
```

### Именование

| Что | Стиль | Пример |
|-----|-------|--------|
| Переменные | snake_case | `project_name` |
| Функции | snake_case | `create_project()` |
| Классы | PascalCase | `ProjectConfig` |
| Константы | UPPER_SNAKE | `DEFAULT_TEMPLATE` |
| Приватные | _prefix | `_internal_helper()` |

### Commits

Используем [Conventional Commits](https://www.conventionalcommits.org/):

```
feat: добавить поддержку Windsurf
fix: исправить создание .gitignore
docs: обновить README
test: добавить тесты для cleanup
refactor: упростить create_project
```

---

## 🔄 Процесс Pull Request

### 1. Создать branch

```bash
git checkout -b feature/my-feature
# или
git checkout -b fix/bug-description
```

### 2. Сделать изменения

- Пиши тесты для нового кода
- Обновляй документацию если нужно
- Следуй стилю кода

### 3. Проверить локально

```bash
# Тесты
pytest

# Типы
mypy src

# Линтинг
ruff check src

# Форматирование
ruff format src
```

### 4. Commit и Push

```bash
git add .
git commit -m "feat: описание изменений"
git push origin feature/my-feature
```

### 5. Открыть Pull Request

1. Перейди в свой fork на GitHub
2. Нажми "Compare & pull request"
3. Заполни описание:

```markdown
## Описание
Что изменилось и почему?

## Тип изменения
- [ ] 🐛 Bug fix
- [ ] ✨ New feature
- [ ] 📖 Documentation
- [ ] 🔧 Refactoring
- [ ] 🧪 Tests

## Чеклист
- [ ] Тесты проходят
- [ ] Документация обновлена
- [ ] Код отформатирован
- [ ] Типы проверены

## Связанные Issues
Closes #123
```

---

## 📁 Структура проекта

```
ai_toolkit/
│
├── src/                     # Основной код
│   ├── __init__.py
│   ├── cli.py               # CLI entry point
│   │
│   ├── core/                # Ядро
│   │   ├── constants.py     # Константы, версия
│   │   ├── config.py        # Конфигурация
│   │   └── file_utils.py    # Утилиты для файлов
│   │
│   ├── commands/            # CLI команды
│   │   ├── create.py        # Создание проекта
│   │   ├── cleanup.py       # Очистка
│   │   ├── migrate.py       # Миграция
│   │   ├── health.py        # Health check
│   │   └── update.py        # Обновление
│   │
│   └── generators/          # Генераторы файлов
│       ├── ai_configs.py    # AI конфиги
│       ├── scripts.py       # Скрипты
│       ├── docker.py        # Docker
│       ├── ci_cd.py         # CI/CD
│       └── git.py           # Git
│
├── web/                     # Web Dashboard
│   ├── app.py               # FastAPI app
│   └── templates/           # HTML шаблоны
│
├── gui/                     # GUI (Tkinter)
│   └── app.py
│
├── plugins/                 # Система плагинов
│   ├── manager.py
│   └── installed/
│
├── templates/               # Шаблоны файлов
│   ├── bot/
│   ├── webapp/
│   └── common/
│
├── tests/                   # Тесты
│   ├── test_create.py
│   ├── test_cleanup.py
│   └── ...
│
├── docs/                    # Документация
│
├── pyproject.toml           # Конфигурация проекта
├── requirements.txt         # Зависимости
└── README.md
```

### Куда добавлять код?

| Что | Куда |
|-----|------|
| Новая CLI команда | `src/commands/` + обновить `cli.py` |
| Новый генератор | `src/generators/` |
| Новый шаблон проекта | `src/core/constants.py` TEMPLATES + `templates/` |
| Новая страница Dashboard | `web/templates/` + `web/app.py` |
| Новый тест | `tests/test_*.py` |

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
pytest

# Конкретный файл
pytest tests/test_create.py

# Конкретный тест
pytest tests/test_create.py::test_create_bot_project

# С покрытием
pytest --cov=src --cov-report=html

# Только быстрые
pytest -m "not slow"
```

### Написание тестов

```python
# tests/test_feature.py

import pytest
from pathlib import Path

from src.commands.create import create_project


class TestCreateProject:
    """Тесты создания проекта."""
    
    def test_create_bot_project(self, tmp_path: Path) -> None:
        """Тест создания бот-проекта."""
        result = create_project(
            name="test_bot",
            path=tmp_path,
            template="bot",
        )
        
        assert result is True
        assert (tmp_path / "test_bot").exists()
        assert (tmp_path / "test_bot" / "bot").is_dir()
    
    @pytest.mark.slow
    def test_create_full_project(self, tmp_path: Path) -> None:
        """Тест создания полного проекта (медленный)."""
        ...
```

---

## ❓ Вопросы?

- 💬 [Discussions](https://github.com/Adrena1ine-ai/AI-Native_Project_Scaffolding/discussions)
- 📱 Telegram: [@MichaelSalmin](https://t.me/MichaelSalmin)

---

Спасибо за участие! 🎉

